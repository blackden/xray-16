# Homebrew dependencies for building OpenXRay on macOS (arm64 / x86_64).
# Mirrors the CI install step in .github/workflows/cibuild.yml.
# Install with: brew bundle install
brew "cmake"
brew "sdl2"
brew "lzo"
brew "libogg"
brew "libvorbis"
brew "theora"
brew "jpeg-turbo"
brew "openal-soft"
brew "ccache"
